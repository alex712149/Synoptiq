import { createFileRoute,Link } from "@tanstack/react-router";import { useSuspenseQuery } from "@tanstack/react-query";import { ArrowRight,CheckCircle2,FlaskConical } from "lucide-react";import { replayEventsQuery } from "@/features/data/queries";import {SectionHeading} from "@/features/shared/SectionHeading";import {TopBar} from "@/features/shared/AppShell";import {DataStatus} from "@/features/shared/DataStatus";import {routeHead} from "@/features/shared/RouteMeta";
export const Route=createFileRoute("/replay")({head:()=>routeHead("Counterfactual Bust Replay","Replay verified SkillBlend forecast cases, including clear wins and scientifically honest misses."),component:Page});
function Page(){const {data:r}=useSuspenseQuery(replayEventsQuery);return <><TopBar mode={r.mode}/><div className="page"><SectionHeading eyebrow="Offline-safe judging path" title="Counterfactual bust replay" copy="No selective storytelling. Every case is compared with the same baselines and the verified truth." action={<DataStatus mode={r.mode}/>}/><div className="replay-list">{r.data.map((e,i)=>{const miss=e.headline.includes("not the single best");return <Link key={e.event_id} to="/replay/$eventId" params={{eventId:e.event_id}} className={`replay-card ${miss?"honest":"win"}`}><div className="case-index">0{i+1}</div><div><span className="kicker">{e.region} / {e.variable.replace("_"," ")}</span><h2>{e.label}</h2><p>{e.headline}</p></div><div className="case-flavor">{miss?<><FlaskConical/>HONEST MISS</>:<><CheckCircle2/>CLEAR WIN</>}</div><ArrowRight className="case-arrow"/></Link>})}</div></div></>}
