export function Metric({ label, value, detail, tone="default" }: { label:string; value:string; detail?:string; tone?:"default"|"good"|"warn"|"honest" }) { return <div className={`metric metric-${tone}`}><span>{label}</span><strong>{value}</strong>{detail && <small>{detail}</small>}</div>; }
export function Meter({ value, tone="primary", label }: {value:number;tone?:"primary"|"warning"|"honest";label?:string}) { const safe=Math.max(0,Math.min(100,value)); return <div className="meter-wrap">{label && <span className="meter-label">{label}<b>{Math.round(safe)}%</b></span>}<div className={`meter meter-${tone}`} role="meter" aria-valuemin={0} aria-valuemax={100} aria-valuenow={safe}><i style={{transform:`scaleX(${safe/100})`}}/></div></div>; }
